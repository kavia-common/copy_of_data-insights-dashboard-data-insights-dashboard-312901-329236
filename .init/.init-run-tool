{
    "command": "bash -lc 'set -euo pipefail; VENV_DIR=\".venv\"; if [ ! -d \"${VENV_DIR}\" ]; then python3 -m venv \"${VENV_DIR}\"; fi; source \"${VENV_DIR}/bin/activate\"; if ! python -c \"import uvicorn\" >/dev/null 2>&1; then echo \"[init-run-tool] uvicorn not importable; installing requirements...\"; python -m pip install -r requirements.txt; fi; export PYTHONPATH=\"$PWD/src${PYTHONPATH:+:$PYTHONPATH}\"; exec python -m uvicorn api.main:app --host 0.0.0.0 --port 3001 --log-level info --timeout-keep-alive 5 --limit-max-requests 1000000'",
    "working_directory": "/home/kavia/workspace/code-generation/copy_of_data-insights-dashboard-data-insights-dashboard-312901-329236/backend_api"
}
